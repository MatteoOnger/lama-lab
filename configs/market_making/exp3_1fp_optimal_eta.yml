experiment_name: "exp3_1fp_optimal_eta"

seed: 0
n_samples: 1000000
history_window: 1

env:
  n_makers: 2
  n_episodes: 100000
  n_rounds: 100000
  epsilon: 0.001

generator:
  _target_: "lama_lab.generators.GaussianMixtureGenerator"
  weights: [1.0]
  means: [0.5]
  stds: [0.1]
  low: 0.0
  high: 1.0

agent:
  _target_: "lama_lab.agents.AgentExp3"
  # sqrt(2 * ln(K) / (T * K)) with K = 15 arms and T = 100000 rounds. That is
  # the single-agent adversarial value; in self-play it is conservative and
  # exp3_1fp.yml uses a larger one.
  eta: 0.002
  gamma: 0.0
  reward_range: [-0.8, 0.5]
  action_space:
    _target_: "lama_lab.analysis.build_quote_grid"
    low: 0.0
    high: 1.0
    delta: 0.2
    epsilon: 0.001

diagnostics:
  n_diag_episodes: 64
  checkpoints: null
